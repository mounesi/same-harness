# MiniMax M3 — flagship MoE (specs conflict: ~230B vs 428B — verify HF card). 4-8x H100.
HF_REPO="MiniMaxAI/MiniMax-M3"                     # TODO verify exact repo id on HF
TP=8                                               # TODO drop to 4 if the smaller spec is right
MAX_MODEL_LEN=262144
EXTRA_ARGS="--trust-remote-code"
